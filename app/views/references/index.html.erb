<div class="row text-center">
  <div class="col-xs-12">
    <h1>Welcome <%= current_user.name %></h1>
    <h2>References Page</h2>
  </div>
</div>

<div class="row top-buffer">
  <div class="col-xs-12">
    <p class="lead">Try to checkout all of the following reference pages</p>
    <p><%= link_to "SQL Injection", references_sqli_path %></p>
  </div>
</div>

<div class="row top-buffer">
  <div class="col-xs-12">
    <h3>TL;DR</h3>
    <p class="lead">If you don't have time to practice exploiting the different types of vulnerabilites
      listed in the references page then quickly read over these general safe coding
      practices. That way you won't accidentally write the vulnerabilities into apps as easily.</p>
    <ul class="list-group">
      <li class="list-group-item">If you have to use string interpolation on a user provided string in your
        custom query, use ActiveRecord::Base::sanitize(input). BE CAREFUL, pay attention
        to the structure of the generated query, you can still get unauthorized reads
        using this method. It isn't the end all be all protection from sql injection.
        (See the <%= link_to "sql injection reference page",
         references_sqli_path(ref_num:0, anchor: "n10") %>
        for the where method for an example)</li>
      <li class="list-group-item">Bullet point</li>
      <li class="list-group-item">Bullet point</li>
      <li class="list-group-item">Bullet point</li>
    </ul>
  </div>
</div>

